%table.row
  %tr
    %td
      %table.twelve.columns
        %tr
          %td.text-pad
            .email-header
              Signup notification for #{@org.name}

            %p New Participants since (#{ @since_time })

            %ol
              - @new_users.each do |user|
                %li= user.full_name

            %p
              = link_to 'Edit notification settings', notifications_url
          %td.expander
